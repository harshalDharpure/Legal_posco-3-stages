.venv/

__pycache__/
*.py[cod]
*.so

# Derived training corpus (auto-built from train + val dialogues)
datasets/merged/final_train_dialogues.jsonl

# Local run outputs: checkpoints, metrics, eval cache, nohup logs.
# Copy to another server with rsync when needed; do not commit large artifacts.
q1_3stage_pipeline/outputs/
